{
    "training": {
        "epochs": 40,
        "learning_rate": 1e-4,
        "patience": 4,
        "batch_size": 64
    },
    "model": {
        "hidden_size": 256,
        "embedding_size": 128,
        "num_layers": 5
    }
}