import torch
import torch.nn as nn
import torchvision.models as models
import pdb

class tweet_model(nn.Module):

    def __init__(self, config, vocab):
        pass

    def forward(self, start = '', train = False):
        pass